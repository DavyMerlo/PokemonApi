import { PokemonTeam } from './PokemonTeam';
import { User } from './User';
import { TeamUser } from './TeamUser';

export type Team = {
    id: number;
    name: string;
    pokemons: number[];
  };
